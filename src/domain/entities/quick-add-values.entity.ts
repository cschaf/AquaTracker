/**
 * @file Defines the QuickAddValues entity.
 * @licence MIT
 */

/**
 * Represents a tuple of three numbers that are used as quick-add shortcuts
 * for water intake on the UI. The values are in milliliters (mL).
 *
 * @example [250, 500, 750]
 */
export type QuickAddValues = [number, number, number];
